{ pkgs ? import <nixpkgs> {} }:

pkgs.mkShellNoCC {
  packages = with pkgs; [
    python313
    (poetry.override { python3 = python312; })
    just
  ];

  shellHook = ''
    poetry install --no-root
    just --list
  '';
}
