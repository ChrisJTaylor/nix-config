# NixOS configs

## First steps!
- Clone the repo
- `CD` into the folder
- run the following command to ensure you have permission to run the scripts:
  - `chmod +x ./*.sh`

## How to restore
Run the `restore.sh` script: `./restore.sh`

## How to backup
Run the `backup.sh` script: `./backup.sh`
